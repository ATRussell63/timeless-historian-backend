version: '3.9'

services:
  timeless_historian_db:
    image: postgres:15-alpine
    restart: always
    shm_size: 128mb
    environment:
      POSTGRES_USER: timeless_admin
      POSTGRES_PASSWORD: example
      POSTGRES_DB: timeless_historian
    ports:
      - "5433:5432"
    volumes:
      - ./init_test_db.pgsql:/docker-entrypoint-initdb.d/init.sql
